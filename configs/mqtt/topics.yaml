mqtt:
  namespace: scorpio

  extract:
    sat: "{namespace}/extract/sat"

  preprocess:
    sat: "{namespace}/preprocess/sat"

  send:
    sat: "{namespace}/send/sat"

  uploaded:
    sat: "{namespace}/uploaded/sat"